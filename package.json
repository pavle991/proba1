{
  "name": "clipshares",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev:web": "turbo run dev:web",
    "dev:mobile": "turbo run dev:mobile",
    "dev:contracts": "turbo run dev:contracts",
    "supabase:start": "supabase start",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.5.0",
    "pnpm": "^8.0.0"
  }
}
